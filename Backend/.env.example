NODE_ENV=development
HOST=0.0.0.0
PORT=3000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=chuyen_phat_nhanh
# Generate a random value with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace-with-a-random-secret-of-at-least-32-characters
CORS_ORIGINS=http://localhost:3000,http://localhost:8081,http://127.0.0.1:8081
# Seed creates demo accounts ONLY when this flag is true. Change passwords for real use.
SEED_DEMO=false
SEED_ADMIN_EMAIL=admin@chuyenphat.vn
SEED_ADMIN_PASSWORD=
SEED_DEMO_PASSWORD=
